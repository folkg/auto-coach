# OpenTofu / Terraform files
*.tfstate
*.tfstate.*
*.tfplan
*.tfplan.*
.terraform/
.terraform.lock.hcl
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json

# Crash log files
crash.log
crash.*.log

# Exclude all .tfvars files, which are likely to contain sensitive data
# such as passwords, ssh keys, and other secrets
# *.tfvars
# *.tfvars.json

# Ignore override files as they are usually used to override resources locally and so
# are not checked in
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# Include override files you do wish to add to version control using negated pattern
# !example_override.tf

# Include tfplan files to ignore the plan output of command: terraform plan -out=tfplan
# example: *tfplan*

# Ignore CLI configuration files
.terraformrc
terraform.rc

# OpenTofu CLI configuration files
.tofurc

# IDE and editor files
.vscode/
.idea/
*.swp
*.swo
*~

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Backup files
*.backup
*.bak

# Log files
*.log

# Temporary files
*.tmp
*.temp

# Environment specific files (keep examples)
# terraform.tfvars (uncomment to ignore)
# environments/*.tfvars (uncomment to ignore)

# State backup files
.terraform.tfstate.lock.info

# Sentinel runtime directory
.sentinel/

# Local values that shouldn't be committed
local.tf
locals_override.tf

# Provider cache
.terraform.d/

# Test files
test/
tests/
*.test.tf

# Documentation build files
docs/_build/
site/
