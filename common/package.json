{
  "name": "@auto-coach/common",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --outDir dist",
    "checks": "bunx biome check",
    "fix": "bunx biome check --fix",
    "types": "tsc --noEmit",
    "all-checks": "bun run fix && bun run types && echo 'All Common checks passed!'",
    "deploy:dry": "bun run all-checks && echo 'Common dry run complete!'"
  }
}
