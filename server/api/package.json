{
  "name": "@auto-coach/server",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "bun run --hot --inspect src/index.ts",
    "build": "bun build src/index.ts --target=bun-linux-x64 --outfile=dist/server --compile --minify --sourcemap --bytecode",
    "build:local": "bun build src/index.ts --outfile=dist/server --compile --minify --sourcemap --bytecode",
    "start": "./dist/server",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "container:build": "docker build -f Dockerfile -t auto-coach-api ../../",
    "container:run": "docker run -p 3000:3000 auto-coach-api"
  },
  "dependencies": {
    "@hono/arktype-validator": "2.0.1",
    "@sendgrid/mail": "8.1.6",
    "arktype": "catalog:",
    "dotenv": "17.2.3",
    "fast-xml-parser": "5.3.4",
    "firebase-admin": "13.6.0",
    "firebase-functions": "6.5.0",
    "google-auth-library": "10.5.0",
    "hono": "catalog:",
    "js2xmlparser": "5.0.0",
    "p-limit": "7.2.0",
    "spacetime": "catalog:"
  },
  "devDependencies": {
    "@types/bun": "catalog:",
    "typescript": "catalog:"
  }
}
